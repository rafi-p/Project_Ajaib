import { Colors } from 'constant';
import { LocalStorage, convert } from 'helpers';

export default {
	genderOpt: [
		{ label: 'All', value: '', id: 1 },
		{ label: 'Female', value: 'female', id: 2 },
		{ label: 'Male', value: 'male', id: 3 },
	],
};
